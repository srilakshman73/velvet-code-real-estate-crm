import CRMDashboardPage from '../page';

export const metadata = {
  title: 'Customer CRM Dashboard | Velvet Code',
  description: 'Real Estate CRM workspace dashboard for client properties, leads, and pipeline.',
};

export default function CRMDashboardAlias() {
  return <CRMDashboardPage />;
}
